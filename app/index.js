import React from 'react';
import ReactDOM from 'react-dom';
import App from './page/FormLogin.component';
ReactDOM.render(
    <React.StrictMode>
      <App />
    </React.StrictMode>,
    document.getElementById('root')
);